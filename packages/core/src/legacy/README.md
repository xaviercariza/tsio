# Legacy core implementation

This folder contains the previous `@tsio/core` implementation before the core type/refactor work.

It is intentionally excluded from `packages/core/tsconfig.json` so the archived implementation remains available for reference without affecting the active package build or type-check.
